import { WorkerOnlyDirective } from './worker-only.directive';

describe('WorkerOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new WorkerOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
